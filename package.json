{
  "name": "form-manager",
  "version": "1.0.0",
  "main": "src/index.ts",
  "dependencies": {},
  "devDependencies": {
    "gts": "^6.0.2",
    "typescript": "^5.8.3",
    "@types/node": "^22.7.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
